import java.io.File;
import java.io.FileNotFoundException;
import java.util.ArrayList;
import java.util.Scanner;

public class HaigusteNimekiri {
    static String[] haigusteLoend = {"astma", "bronhiit", "diabeet"};

    /* loen sisse failist haigused listi
    static ArrayList<String> haigusteLoend = new ArrayList<>();
    File failiSisu = new File("src/RiskigruppideKlassifikaator.txt");
        try(
    Scanner sc = new Scanner(failiSisu, "UTF-8")) {
        while(sc.hasNextLine()) {
            haigusteLoend.add(sc.nextLine()); // haigused lisatud failist listi
        }
    } catch(
    FileNotFoundException e) {
        e.printStackTrace();
    }
*/

}
